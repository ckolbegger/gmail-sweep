{
  "name": "gmail-sweep",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "dev:backend": "npm run dev -w packages/backend",
    "dev:terminal": "bun run packages/terminal/src/index.ts"
  }
}
